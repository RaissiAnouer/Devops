name: chrone
on:
    schedule:
        - cron: * * * * *

jobs:
    hello_every_5_min:
        runs-on: ubuntu-latest
        steps:
            - name : hello
              run: echo "hello" ${{github.actor}}
